## 1. popup 介面：票種改為選填輸入框
- [x] 1.1 在 popup.html 將票種下拉選單（id="ticket-type-select"）改為單行文字輸入框（input type="text"），保留 label「票種」
- [x] 1.2 將輸入框設為選填（無 required），並加上適當 placeholder（例如：「選填，輸入票種關鍵字」）
- [x] 1.3 在 popup.js 改為取得票種輸入框的 value（trim 後字串），不再使用 select 的 value

## 2. 訂票流程：票種選擇優先順序與關鍵字
- [x] 2.1 訂票按鈕事件中將票種輸入框的 value（trim）傳入票種選擇邏輯（例如傳給 selectTicketType 或等同函數）
- [x] 2.2 實作選擇邏輯：若關鍵字非空，優先從解析出的票種中尋找「名稱含有該關鍵字」的票種（取第一個符合者）
- [x] 2.3 若關鍵字為空或無符合者：依序尋找名稱含「全票」→ 名稱含「單人套票」→ 否則選第一個票種
- [x] 2.4 移除「優先選擇 price 為 999」的邏輯
- [x] 2.5 若依上述規則仍無可用票種，顯示錯誤訊息（如「無法找到可用票種」）並中止後續操作

## 3. 驗證與文件
- [x] 3.1 手動驗證：不輸入關鍵字時，行為為全票 → 單人套票 → 第一個
- [x] 3.2 手動驗證：輸入關鍵字時，優先選到名稱包含該關鍵字的票種
